<%= render 'layouts/error',book: @book %>

<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
	   <div>
	<h4>Title</h4>
	<%= f.text_field :title %>
		</div>
	   <div>
	<h4>Opinion</h4>
	    <%= f.text_area :body %>
	    </div>
	    <div><h4> </h4></div>
		    <div>
	    <%= f.submit "Update Book",class: "btn btn-smbtn-primary btn-block active"  %>
	     	</div>
	   		<% end %>
	    </div>
	    <div>
			<%= link_to "Show", book_path(@book) %><br>
			<%= link_to "Back", books_path %>
				</div>