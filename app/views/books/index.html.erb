<h1>Books#index</h1>


<% if @book.errors.any? %>
		  <div class="alert alert-danger">
		  	<h2><%= pluralize(@book.errors.count, "error") %> prohibited this obj from being saved: </h2>
		  	<ul>
		  		<% @book.errors.full_messages.each do |message| %>
		  		  <li><%= message %></li>
		  		<% end %>
		  	</ul>
		  </div>
		<% end %>
<div>
	<div class="container">
<h2>New book</h2>
<%= form_for(@book) do |f| %>
	   <div>
	<h4>Title</h4>
	<%= f.text_field :title %>
		</div>
	   <div>
	<h4>Opinion</h4>
	    <%= f.text_area :body %>
	    </div>
	    <div><h4> </h4></div>
		    <div>
	    <%= f.submit "Create Book",class: "btn btn-smbtn-primary btn-block active"  %>
	     	</div>
	   		<% end %>


	    <div class="col-xs-9">
			<h2>Books</h2>
		    <table class="table">
		        <tr>
			    	<th>image</th>
				    <th>Title</th>
				    <th>Opinion</th>
			    </tr>
			    <% @books.each do |f| %>
			     <tr>
			    	<td><%= link_to attachment_image_tag(f.user, :profile_image, :fiil,60,60, format: 'jpeg', fallback: "no_image.jpg"), user_path(f.user) %></td>
				    <td><%= link_to f.title, book_path(f) %></td>
				    <td><%= f.body %></td>
			    </tr>
			    <% end %>
			</table>
	    </div>
	</div>