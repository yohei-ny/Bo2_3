<div class="container">
    <div class="row">
    <div class="col-xs-3">
    	<%= flash[:notice] %>
<%= render 'info', user: @user %>

<%= render 'books/form', book: @book %>
</div>
<div class="col-xs-9">
<%= render 'books/table', books: @books %>
			</table>
		</div>
	</div>
</div>
